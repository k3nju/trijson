#pragma once
#include <stdint.h>

namespace trijson
	{
	bool ParseEscapeString( char in, char &out );
	uint32_t StrToUint32( const char *in, size_t size, uint32_t *out );
	uint32_t EncodeToUTF8( uint32_t in, uint8_t *out );
	}
